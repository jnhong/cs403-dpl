size := 5 :;

a := allocate(size) :;

i := 0 :;


while[ : i < size :  a[i] == i * i, i == i + 1 ],

i == 0,

while[ : i < size :
       println("a[", i, "] is ", a[i]),
       i == i + 1
     ],

! if you forget the size...

println("\nthe size of a is ", sizeof(a)),
